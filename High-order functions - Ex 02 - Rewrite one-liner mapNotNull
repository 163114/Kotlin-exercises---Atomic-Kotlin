//fun List<Int>.transform(): List<Int> =
//    filter { it % 2 == 0 }.map { it * it }

fun main(){
    val listOne = listOf<Int>(1, 2, 3)
    val listTwo = listOne.mapNotNull { it * it }
    println(listTwo)
}
